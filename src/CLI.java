import java.util.Scanner;

public class CLI {
	
	public static void main(String[] args) {
		
		// TODO: parse command line arguments as needed
		
		Scanner scin = new Scanner(System.in);
		String line;
		int stage = 0;
		while (scin.hasNextLine()) {
			line = scin.nextLine();
			
			// TODO: decide what the input should be
		}
	}
	
}
